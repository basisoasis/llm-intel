{
  "name": "llm-intel",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist",
    "generate": "bun scripts/generate/generate-model-ids.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "bignumber.js": "^9.3.1",
    "valibot": "^1.2.0"
  }
}
